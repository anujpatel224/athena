import ClientList from '../components/ClientList';

export default function Conferences() {
  return (
    <main>
      {/* ... existing code ... */}
      
      {/* Replace the existing client list section with the new component */}
      <ClientList />
      
      {/* ... rest of the existing code ... */}
    </main>
  );
} 